# Shared wallet implementation record

Implementation started from Center main `d5ec4af1877cca8d1dda806cdd217aa18b16b3b6` in the isolated `feat/shared-passkey-wallet` worktree. An inventory of local branches and worktrees confirmed that existing Center source work was already represented on main, including squashed deployment and sponsorship changes. Historical dev ancestry does not need merging. No remote push or production activation accompanies this checkpoint.

This is an experimental Base account profile and its verification foundation. It is not yet a usable shared wallet. The [strategy](CENTER-WALLET-PLAN.md) and [specification](CENTER-WALLET-SPEC.md) remain the delivery contract.

## Implemented boundaries

| Area | Working behavior | Remaining boundary |
| --- | --- | --- |
| Required observations (W0) | Node 22.16+, writable PostgreSQL 16, pinned Foundry and nonempty required suites with no skips. Atomic reports, redacted bounded logs, child deadlines and source fingerprints. | A successful focused test or preflight is not the complete gate. Read the report for the tested commit. |
| Cryptographic experiment (W1) | Actual upstream P256/FCL signer, Safe 1.4.1, Safe7579, EntryPoint 0.7, separate SafeMessage/SafeOp preimages, bounded dynamic owner signatures and negative vectors. | Local Cancun/native-value execution does not prove Base provider admission, V6/USDC payments or device compatibility. |
| Account authority (part of W2) | Explicit `center-passkey-v1`, one immutable passkey signer plus one EOA, threshold one, Base Safe as stable API principal. Canonical runtime/configuration evidence, deployed-wallet setup and rotation reject stale authority. | Enrollment proves control of both pilot owners. Operational independence and consumer recovery still need real-user acceptance. No production manifest selects this profile. |
| Atomic creation (part of W2/W4) | Explicit `center-passkey-bootstrap-v1` predicts the immutable signer and Safe, then creates both through exactly two initializer operations. Canonical inspection verifies the unique direct factory transaction, historical/current code and complete creation trace. Profile-specific checkpoints preserve original creation evidence through rotation. | Deployment transport remains to be connected to the journal. Generic MultiSend and EntryPoint-nested factory provenance remain unsupported by this profile. |
| Operation preparation/submission (part of W2) | Bounded maximum assertion for estimation, distinct signing view, exact contract-owner verification and unchanged approved operation fields. An estimate of the actual signed bytes must fit approved gas before the durable claim; its margin is not applied twice. Legacy EOA/session paths keep their existing meaning. | Browser policy and durable ceremony consumption must be connected before this is a browser wallet. Provider estimates are point-in-time observations, and actual provider acceptance remains its own gate. |
| Registration and assertion verification (part of W3) | Bounded none/ES256 registration parsing with directly pinned CBOR decoding, plus genuine P256 assertion verification with exact signed JSON, expected RP/origin/challenge, UP/UV, credential/user handle, backup flags and byte limits. Synced counters are observed without requiring monotonic nonzero values. | The pure registration parser produces an unproven candidate. Neither pure function creates a session or account. |
| Durable enrollment (W3 storage) | A server-owned intent fixes RP/origin, stable user handle, manifest, backup owner and creation salt. Registration freezes the candidate and exact initializer; a fresh passkey assertion and backup EOA signature bind the same enrollment document. One PostgreSQL transaction consumes the proof and records the unique verified credential and original receipt. | Verified enrollment is ownership evidence, not deployed-wallet status, a session, an API grant or deployment approval. Public HTTP admission, cookies and CSRF are not exposed. Consumer onboarding without an existing backup wallet remains a product gate. |
| Deployment approval and transaction codec (part of W4) | A separate fresh deployment document binds the verified enrollment and exact factory call. The pure validator rejects changed EIP1559 fields, noncanonical raw encoding, and transactions outside the relay policy. Recovery validation preserves the original bytes after approval expiry. Actual browser assertions cannot substitute enrollment and deployment purposes. | The pilot codec requires a positive priority fee. Its execution-cost bound excludes Base L1 and operator charges. See the [deployment strategy](WALLET-DEPLOYMENT-STRATEGY.md). |
| Deployment journal and observations (part of W4) | One PostgreSQL pool retains a permanent allocation, sender nonce and exclusive lane. A fresh claim consumes approval against the current credential. Signing leases and exact-byte persistence select one durable winner. Revision/hash compare-and-swap stores bounded latest and retained canonical observations; a bounded cursor scans unresolved operations. | Phases remain prepared/claimed/signed; signed is an artifact state. Historical evidence is not current chain truth. No signing transport, broadcast, automatic refill, lane release or public route is enabled. A single unresolved lane does not establish throughput. |
| Read-only deployment chain checks (part of W4) | Bounded preflight checks the exact factory call, canonical block, pinned dependencies, owners, nonce, balance, simulation and execution fees. `observeSigned` instead inspects immutable stored bytes, exact transaction/receipt/finality and full wallet creation/current authority, without redoing fresh admission or writing an index. | Missing evidence remains unknown. Treasury outcome and wallet readiness are separate. Complete Base affordability is unknown and dispatch is ineligible. No funded provider observation was made. |
| Base fee experiment (part of W4) | Pure bounded FastLZ and checked bigint calculations use the complete signed type2 bytes for Fjord/Isthmus and Fjord/Jovian parameters. Actual pinned Solidity contracts on Anvil independently check the results. | Parameters are supplied observations, not verified live Base configuration. No deployed proxy/runtime/fork profile is selected; future inclusion fee ceiling remains unknown. This experiment grants no dispatch or allocation authority. |
| Shared trust policy (part of W6) | Center's existing origin configuration gains explicit callback metadata. PostgreSQL revision activation advances changed/removed app generations while retaining unrelated apps. Grant admission and use check shared active policy under transactional locks. There is no per-site consent flow. | Callbacks are empty by default. Nothing activates on process startup. Central login and PKCE must establish authority before public grant issuance. |
| Typed app authority (part of W6) | Separate app-grant storage, a shared UUID namespace and immutable database incarnation preserve legacy bot semantics. Genuine signed requests bind the request key, API audience and origin. Durable checks enforce policy, expiry, revocation, canonical readiness and authority/logout epochs. Stable account/origin quotas and separate owner signatures protect Base UserOperations; app keys cannot use legacy spending or SmartSessions paths. | No public issuance, central browser session, cookies or PKCE handoff exists yet. Unobserved external owner rotation is not immediate database revocation. |
| Canonical authority reconciliation (part of W6) | An internal service loads verified enrollment, current credential and live setup binding, then observes configured Base contracts outside database locks. Full creation/current-owner evidence, authority history and separate session-administration history determine identity. Transactional reconciliation initializes or advances epochs, fences proven canonical conflicts and grants at most 30 seconds of readiness. App admission also checks the currently live binding. | No refresh worker, login proof, browser session or public issuer exists. Explicit internal refresh is required; a one-hour app grant does not provide one hour of usable readiness. Live Base history availability and provider capacity remain unobserved. |
| Ceremony persistence (part of W3) | PostgreSQL transactions, exact replay/conflict decisions, database-clock expiry, bounded retained receipts and two real process crash/race tests. | A consumed challenge is a receipt, not a payment dispatch or session. Enrollment and operation services must connect their own durable transitions. Capacity and recovery admission need application-level controls and pressure qualification. |
| Deployed-wallet setup (part of W2/W4) | Versioned SafeMessage approval and separate browser-key proof reuse atomic account/binding/grant storage. Real EVM setup and backup-owner rotation preserve the same Safe identity. | No public setup endpoint, registration or sponsored first deployment is enabled. |

The pilot recovery configuration grants either owner full authority. Direct Safe withdrawal of its native balance works in the local experiment with Center unavailable and EntryPoint, adapter, signer and verifier code unavailable. It does not recover an EntryPoint deposit while EntryPoint is absent. Enrollment tests prove backup-key possession with a test EOA; consumer backup usability and independent storage remain unaccepted.

An unproven registration candidate does not reserve its credential ID globally. The verified mapping has unique RP/credential and current-primary wallet constraints, plus a deferred foreign key to its verified enrollment/account/user-handle tuple. Verification and challenge consumption roll back together when a concurrent credential claim loses. Verified identities and their original receipts survive ceremony expiry and cleanup; replay cannot restore a superseded primary credential. Pending-enrollment admission counts pending records only, while verified identities require durable storage capacity. A fresh deployment approval must resume the same verified enrollment and exact initialization.

These rotation proofs reject removed onchain owners and old setup approvals. Canonical reconciliation now connects observed authority changes to atomic authority and logout epoch changes while preserving the Safe account ID. Explicit registration of a replacement credential for that same Safe, consumer recovery and central browser sessions remain W5/W6 work. A changed owner does not automatically become a verified enrollment or regain readiness.

## Gate A status

| Question | Status and evidence |
| --- | --- |
| Exact signature paths | Local cryptographic proof passes. TypeScript assertion/codec output executes against pinned contracts on Anvil; Foundry adds 256 real P256 challenge-substitution cases and atomic creation tests. See [stack evidence](../../src/rest/smartAccounts/stack/passkey/README.md). |
| Complete account model | Local atomic creation, canonical inspection, stable identity, deployed-account setup and rotation pass. The [bootstrap experiment](../../src/rest/smartAccounts/stack/passkey/bootstrap/README.md) selects a direct factory transaction for the next relay integration; the initial EntryPoint route exceeds the tested 500,000 verification-gas bound and has canonical mempool restrictions. Durable sponsored first deployment remains W4 work. |
| Sponsorship fit | Payload bounds, real FCL execution and local EntryPoint behavior are measured. Live Base bundler/paymaster acceptance and fee behavior are pending. |
| Device and origin fit | Chromium virtual-authenticator suites pass actual creation, parsed-candidate possession, discoverable user-handle matching, negative assertions and durable enrollment through two HTTP processes. The local Mac probe now passes in Safari and installed Chrome with real passkey prompts. Production RP, mobile-browser and native compatibility remain pending. `wallet.juicebox.center` is still proposed; an earlier paired-iPhone attempt was unreachable, and Beep's native associations still target Para. |
| Independent exit | Local native-balance withdrawal and same-address owner replacement pass. Consumer recovery selection, independent possession and physical-device loss/restore remain pending. |

Gate A is not fully accepted. No durable production passkey, real deposit, migrated Para account or native compatibility claim follows from these tests.

The user has a Mac, iPhone and iPad available for physical testing. The first device matrix is Mac Safari/Chrome and iPhone/iPad Safari, including passkey sync where their Apple account settings permit it and Beep's iOS handoff. Availability is not test evidence: per-run device/browser attribution, sync, completed passkey prompts and native transitions still need recorded outcomes. Android Chrome remains a separate compatibility requirement; no Android device has been offered yet.

Read-only inventory on the current Mac reports macOS 15.7.3 (24G419), arm64, Safari 26.0.1 and Chrome 152.0.7977.83. This records installed versions only. The automated virtual-authenticator suite uses its separately pinned Chromium build; neither inventory nor that suite establishes successful physical-device enrollment.

`npm run wallet:device-probe` starts a temporary Mac-local page at the printed `http://localhost:<port>` address. It uses the `localhost` RP to create a test discoverable passkey and verify a separate fresh possession assertion. A Passkey name field accepts a memorable Unicode label up to 64 UTF-8 bytes, supplies the provider display metadata and stays fixed through that ceremony. Equal labels still receive separate random user handles; names are excluded from server events and status counters. The server retains bounded ceremony state in memory and stops after 15 minutes or Ctrl+C; it creates no Center account, session or payment. A test credential can remain in the device's passkey store after the server stops. This probe does not establish production RP, iPhone/iPad sync or native handoff compatibility.

On 2026-09-13 the user confirmed a successful Safari creation/possession roundtrip, consistent with the local server counter and supplied screenshot. A visible, instrumented Chrome 152.0.7977.83 roundtrip then passed registration and fresh possession verification at 22:08:41 UTC with the browser sandbox enabled and no virtual authenticator. The successful response matched the created credential, user handle, origin and fresh challenge and required user verification. The authenticator device/provider used for each prompt was not recorded; this does not establish iPhone/iPad Safari, sync or native handoff. Sanitized evidence is under `.generated/wallet-observations/device-probe/physical-summary.json` and `physical-chrome-events.ndjson`.

The earlier Chrome 403 was reproduced and classified: the browser returned a different saved credential and user handle, while its origin, challenge and UV checks matched. The verifier correctly rejected the substitution. The probe now distinguishes non-interactive status from buttons, explains a wrong-passkey selection, and supports a fresh retry. Automated two-passkey discovery tests reproduce rejection and recovery without weakening exact credential matching.

## Observability loop

Run [the required gate](CHECK-OBSERVATIONS.md) with Node 22, pinned Foundry and a disposable PostgreSQL 16 database:

```sh
npm run check:preflight
npm run check
```

The command prints `.generated/checks/<run-id>/summary.json`. Inspect its result, revision, dirty state, source attribution, individual step results and required-suite counts. The source must remain frozen during a full run. Logs and reports are ignored local artifacts; no secret environment values belong in committed fixtures. Read the failed step, reproduce the failure in its focused suite, fix it, then rerun the affected checks and the integration gate.

The observation runner now hashes actual file contents and modes against committed blobs, so Git clean filters and hidden index flags cannot conceal changes. Browser reports and screenshots are under `.generated/wallet-observations/browser-required/` and `browser-enrollment/`; their tier explicitly excludes physical-device claims. These are the last focused observations, not standalone claims that the full required gate passed.

Behavioral failure evidence includes the legacy EOA envelope rejecting contract signatures, incorrect base64url padding rejected by the actual upstream signer, legacy onboarding-method substitution rejected by the new profile, the old PostgreSQL authorization constraint rejecting the new explicit method, quota contention and process death. The signature ordering guard was removed in an isolated mutation copy; two tests then failed. These establish specific assertions, not complete test-suite mutation coverage. Initial missing-import failures are not counted as behavioral proof.

Two actual child processes exercise transaction rollback before commit and recovery after commit when the response is lost. Actual invalid EntryPoint submission is checked for no payment, deposit debit or nonce consumption. A local simulation alone is not recorded as a submitted transaction.

The enrollment browser gateway drops its response only after the replica returns from an awaited PostgreSQL commit. Chromium transparently retried that interrupted POST three times in the observed run. Every result referred to the original receipt, and a request through the second replica recovered the same identity. Another real assertion with user verification disabled failed without a credential mapping; a later verified assertion completed enrollment.

The enrollment race suite offers 100 identical candidate requests, 100 identical finalization requests, and 100 conflicting candidates across two actual processes with one database connection each. Identical requests retain one candidate and one original verification receipt; conflicting candidates produce one acceptance and 99 explicit conflicts. Reports under `.generated/wallet-observations/enrollment-races/` include stage latency and status counts. These bounded races do not qualify sustained throughput, overload recovery or deployment capacity.

Real PostgreSQL tests exposed an unsupported oversized regex quantifier during credential insertion and missing-field SQL checks that could evaluate to NULL. The migration now uses a bounded length check plus character validation, and requires JSON-dependent constraints to evaluate to true. The process fixture also needed callback-compatible pool reads; its initial hang was test infrastructure, not a production store deadlock.

An expiry test observes an actual PostgreSQL uniqueness-lock wait, releases the blocking insertion after the deadline, and verifies that the waiting finalization rolls back credential insertion and challenge consumption. Isolated mutations removing backup-owner equality and weakening RP/credential uniqueness both made their targeted tests fail; neither mutation changed the shared source tree.

Deployment codec tests first failed against a compiling registration-context/general-relay baseline, then passed with the distinct deployment document and exact envelope validation. Installed transaction parsing and signer recovery accepted several noncanonical RLP forms; canonical byte comparison rejects them without changing the bytes returned for later dispatch. A separate mutation test exposed caller-owned context changing during assertion access; proof verification now snapshots bounded trusted state first.

The general signed-transaction validator now accepts canonical zero fee quantities for legacy, EIP2930 and EIP1559 while rejecting malformed integer encodings, empty or nested signature scalars, and nested access-list storage keys. A genuine signature over a nested key can survive SDK parsing, reserialization and signer recovery; explicit address/key byte-string checks close that separate boundary. Valid nonempty access lists, including leading-zero keys, retain their original bytes. The deployment profile's positive-priority policy is unchanged.

Deployment journal tests observe real unique-index blocking across approval expiry, kill either process at durable boundaries, race differing valid signatures and retain the original nonce/allocation after recovery. Direct SQL cannot insert a claimed/signed row or skip the required phase transition. A post-write lease expiry rolls back signed-byte persistence. Shared-policy tests use two processes to race activation and callback removal against claims; a regression with different leading search-path schemas exposed an advisory-lock identity bug. Activation now locks by the resolved policy table OID. Input snapshots reject accessor/proxy changes across connection and lock waits.

Signed deployment observations run in one disposable bounded RPC scope. Actual Anvil cases cover complete cold wallet inspection, third-party deterministic creation, pending and reverted treasury transactions, owner changes and missing evidence. The successful cold fixture used 92 RPC calls and a 21,020-byte trace; those measurements do not establish live Base history availability. The observer never writes creation checkpoints or calls a history index. Migration 018 retains a bounded latest observation and historical canonical evidence using signed-hash/revision compare-and-swap. Direct SQL regressions exposed missing-field SQL NULL acceptance, contradictory finalized classification and independent revision reset; the guards now reject each. Unknown or reorg observations preserve liabilities and historical evidence without relabeling that evidence as current truth.

The Base fee experiment has behavioral failure evidence against compiling stubs, published signed-envelope vectors and a separate pinned Solidity reference on Anvil. Adding the unsigned estimator's 68-byte padding to signed bytes fails both published expected outputs. Supplied fee parameters determine a snapshot calculation only. See the [reference provenance and limits](../../src/rest/wallet/stack/baseFees/README.md).

Migration 019 keeps app grants out of `rest_bot_grants` and reserves both grant kinds in `rest_grant_ids`, including writes by old binaries. The signed request still carries its original UUID; the internal app actor includes a database-generated bigint incarnation. Cleanup retains grants beyond signed-request replay lifetime and never deletes business-operation history. Reusing a UUID creates a new incarnation, so an old actor cannot acquire renewed authority. A global-logout epoch is distinct from an individual browser session. An older database restore can rewind epochs and sequences and still needs a separate restore reconciliation fence.

Migration 020 adds canonical authority evidence and readiness. The new identity binds the immutable enrollment, current verified credential, live setup binding and original creation transaction to the full account `stateHash` plus `sessionAdministration.{epoch,hash}`. Separate session-administration history detects enable/remove cycles even when the current session list returns to empty. Legacy account hashes remain unchanged; ordinary Safe/EntryPoint nonces and session use counters do not change this identity. The producer uses a disposable inspector with bounded RPC calls and complete history, without checkpoint writes or a history-index synchronization.

An absent authority row initializes at epochs 1/1 only after a complete matching observation. Existing epoch-only rows migrate unready; their first matching reconciliation increments both existing epochs once instead of resetting them. The producer rechecks the previously accepted canonical block even when the new head is higher. A proven replacement fences authority and advances both epochs once per conflict episode. Unknown evidence removes readiness without clearing that fence. Recovery requires a complete new-branch identity candidate, followed by a fresh observation proving the same candidate anchor and identity; clearing the fence retains the advanced epochs so old grants cannot revive.

Readiness expires at most 30 seconds after the observation began, with canonical head-age bounds checked again at persistence. PostgreSQL compares the captured context under account, enrollment, authority and credential locks and uses revision/epoch compare-and-swap, including checks after writes. The app guard compares the live binding ID and authorization digest under the account lock, so revoking or replacing the binding immediately blocks admission. The internal service captures its dependencies, never retries a stale reconciliation with new epochs, and persists a completed observation even if the requester subsequently cancels. Exact receipt replay does not renew readiness. No RPC or cryptographic proof work runs while those locks are held, and no automatic startup initializer or refresh worker is enabled.

Focused pure authority, RPC-boundary, PostgreSQL, internal-service and actual Anvil suites cover initialization races, logout ordering, expiry after locks/writes, owner and session-administration changes that return to their original state, and a replacement fork with a higher head. The actual cold authority fixture used 86 RPC calls initially and 87 on refresh, with a 21,040-byte bootstrap trace. These measurements prove that fixture fits the bounded scope, not live Base availability or throughput. The new authority and device-probe suites are registered in the required inventory; their focused observations are separate from the completed full-gate report required for any tested commit.

Two actual HTTP processes verify genuine EIP712 app requests against one PostgreSQL database. Tests cover nonce contention, audience/origin/signer substitution, logout, revocation, policy change and durable actor preservation. Pure and service tests reject app-to-bot relabeling, SmartSessions, cached disallowed operations and request-key spending; a separate owner signature still admits an exact Base UserOperation. Browser-safe scope/error primitives remain in a shared leaf module so the existing browser client can build without importing Node-only authority validation. A generic callback cannot provide transaction-local app admission and therefore rejects app actors; durable PostgreSQL stores use the shared transaction helper.

The long-history test exposed an unpinned Anvil hardfork: its 32,000-block fixture spent roughly 115 seconds mining and 75 milliseconds inspecting. Selecting the same explicit Cancun target as the pinned contract proofs reduced mining to a few seconds. The 32,000 actual blocks, retained history, concurrency assertions and three-second inspector deadline are unchanged. This is a fixture correction, not a service throughput measurement.

## Next dependency

Connect immutable signed-transaction observations to bounded signing transport and fenced dispatch/recovery. Complete reviewed Base fee provenance and allocation policy are required before dispatch can be enabled. Stored observations preserve the original nonce, fees, bytes and liabilities; they do not release a lane. The local EntryPoint bootstrap experiment is not a hosted-bundler admission claim.

A bounded fair authority-refresh loop, same-Safe replacement-credential enrollment, shared Center login, operational trust-policy activation, PKCE app handoff, exact review UI, the shared connector, Beep/Money payments and native integration remain subsequent work. The internal authority service alone cannot keep grants usable beyond its 30-second readiness window. The strategy's sustained/burst/soak targets have not been run; concurrent correctness tests establish no throughput or production capacity claim. Keep legacy Para and external-wallet behavior available while these gates are completed.
